/* eslint-disable no-useless-catch */
/* eslint-disable no-undef */

// const sinon = require("sinon");
// const chai = require("chai");

// const HRSBoilerplateController = require("../controllers/hrsBoilerplateController");
// const logger = require("../winston");

// const { expect } = chai;
/**
 * Seed the database with products.
 */
// const createHRSBoilerplateItems = async () => {};

/**
 * Connect to a new in-memory database before running any tests.
 */
before(async () => {});

/**
 * Seed the database.
 */
beforeEach(async () => {});

/**
 * Clear all test data after every test.
 */
afterEach(async () => {});

/**
 * Remove and close the db and server.
 */
after(async () => {});

describe("HRSBoilerplateController DB calls", () => {
  it("should work without error");
});

describe("HRSBoilerplateController => fetchHRSBoilerplateItems", () => {
  it("should return 200 OK with correct input");
  it("should return 500 Error with invalid input");
});

describe("HRSBoilerplateController => fetchHRSBoilerplateItemById", () => {
  it("should return 200 OK with correct input");
  it("should return 500 Error with invalid input");
});

describe("HRSBoilerplateController => addHRSBoilerplateItems", () => {
  it("should return 200 OK with correct input");
  it("should return 500 Error with invalid input");
});

describe("HRSBoilerplateController => patchHRSBoilerplateItem", () => {
  it("should return 200 OK with correct input");
  it("should return 500 Error with invalid input");
});

describe("HRSBoilerplateController => deleteHRSBoilerplateItemById", () => {
  it("should return 200 OK with correct input");
  it("should return 500 Error with invalid input");
});
